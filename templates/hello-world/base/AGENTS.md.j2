# AGENTS.md — {{ project_name }}

This projects was scaffolded by `usta` from the `hello-world` template.

## Quick orientation

- Project name: `{{ project_name }}`
- Tagline: {{ tagline }}

## Working with this project

This is a minimal scaffold. Real templates (e.g. `nx-monorepo`) ship richer
agent guidance describing the architecture, package managers, and test
commands.

## Updating

Run `usta update` from this directory to re-render the template at its
current version against the answers stored in `.usta/snapshot.toml`.
