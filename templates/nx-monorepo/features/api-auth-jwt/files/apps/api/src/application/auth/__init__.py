"""Auth application layer — placeholders for token issuance/verification.

Concrete user repositories live in `src.infrastructure` once chosen; this
module declares protocols the routers depend on.
"""
