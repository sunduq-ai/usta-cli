"""Application layer: use cases, command/query handlers.

Code in this package depends on `src.domain` and protocols defined here,
**not** on `src.infrastructure` directly. Concrete adapters are wired in
`src.api.dependencies`.
"""
