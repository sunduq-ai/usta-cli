edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true

# `imports_granularity` and `group_imports` are nightly-only; we omit them
# so stable rustfmt is silent. Reintroduce if/when the project moves to a
# nightly toolchain for fmt.
